# Ticket 11 Spec review

Axis: Spec only\
Ticket: `.scratch/execution-context-propagation/issues/11-composition-and-consumer-qualification.md`\
Worktree: `/private/tmp/execution-context-frontier-20260905/11`\
Branch: `codex/execution-context-20260905-11`\
Fixed base: `04c66e84e4b05814e462ba4999759b571109e9cf`\
Reviewed head: `e055adb0148d8be69b42e2874a38641efa512e04`\
Reviewed tree: `3eafab29e06b9f901c07070bace13f2b1d86f538`\
Result: PASS — 0 findings; worst severity: none\
Requested reviewer profile: `gpt-5.6-sol/high`; effective runtime profile was not exposed and remains unverified.

## Range integrity

The supplied fixed range resolves to one commit and exactly five files: the two Notes composition tests, the new propagation guide, the Notes README, Product Surface, and the allocated ticket-11 section of ADR 0037. `HEAD` equals the reviewed head and `git status --porcelain=v1` is empty. The worker report records all six direct prerequisites and their transitive predecessors as ancestors of the base; the tracker map independently shows tickets 01–10 resolved and ticket 11 as the current claimed frontier. Ticket 11's `Status: claimed` is a tracker ownership/lifecycle state, not a claim that review, gate, final acceptance, or the parent lifecycle has passed.

No production Java, POM/BOM, harness, parent spec, original finding, or tracker file changed in the range. The worker report binds the committed binary diff to SHA-256 `1e22a17300dc918e852f0b33be060b362ef8514e945142b58c2781fb130800fe` and records a clean exact-head patch. I did not rerun checks already recorded green and did not read or use the independent Standards report.

## Requirement disposition

No missing, partial, incorrectly implemented, or out-of-scope requirement was found.

- Requirement: “逐项关联 spec 的 US01–US22/T01–T09 到实际前置票、消费者、命令及结果；缺失证据回到责任票，不在汇总中推断通过.” The worker report provides complete US01–US22 and T01–T09 tables tied to the C01–C15 command registry. It separates predecessor-owned evidence from ticket-11 common-baseline evidence and does not turn the earlier ticket-06 topology run into current-head proof. C12 is the common-source affected consumer run; C15 is now current-head Scenario evidence.
- Requirement: verify the bounded combinations “可信入口→直接异步任务→业务读取”, “注册回调→外部线程完成→业务读取”, and “AI 请求→工具循环→流式响应”, with Reactor separately consumed through its native template. The Notes test opens an explicit trusted Holder context only around direct submission, then reads through the production `NoteApplicationService` and PostgreSQL repository on the decorated worker (`NotesDemoIT.java:405-462,504-520`). The callback is captured at registration, completed later by the SDK-shaped external worker, performs the same Application Service read on that exact completion thread, and restores the worker before `complete` returns (`NotesDemoIT.java:464-557`). ADR 0037 expressly labels this as the trusted-Holder seam and disclaims an HTTP-to-async chain (`docs/adr/0037-use-explicit-restorable-execution-context-snapshots.md:331-340`). The existing AI consumer supplies the real two-round `ChatClient`/`ToolCallingAdvisor`/stream composition and the Reactor-only consumer remains a separate native-Context qualification; C12 reran both at the common source state.
- Requirement: “组合场景覆盖不同租户、同租户不同请求、Platform 与缺失的适用分支，证明业务内部上下文正确及退出后恢复；未授权/不足上下文拒绝不能产生相关业务或消息副作用.” Direct-task coverage includes Tenant A with two complete but distinct Actor/Initiator/Correlation identities, Tenant B, cross-tenant rejection, Platform rejection, and absence (`NotesDemoIT.java:416-453,580-595`). The callback composition separately covers two same-tenant request identities (`NotesDemoIT.java:472-493`). Each successful business observation asserts the exact captured `ExecutionContext` object, each success or failure is followed by a probe of the same single worker's pre-existing foreign Platform context, and final assertions restore the calling thread to absence. Note, Outbox, and receipt counts are unchanged after cleanup (`NotesDemoIT.java:458-461,498-501`). Existing HTTP/message negative side-effect obligations remain separate C06/C10/C15 evidence rather than being inferred from read-only Notes branches.
- Requirement: every public capability must have an actual independent consumer and must not add a formal Platform/AI business service or test-only production endpoint. The range adds only consumer IT code and documentation. Notes uses public Kernel APIs plus its existing production Application Service and PostgreSQL Adapter; Spring task, Reactor, and Spring AI remain their independent Maven consumers. No production Java or endpoint was added.
- Requirement: actual dependency closures and precise artifact identities must prove Kernel-only, selected Spring, Reactor-only, and AI isolation “不只查看 POM.” C13's saved runtime trees resolve Kernel with no runtime dependency, Spring task with Spring Framework 7.0.9 and no Reactor/AI, Reactor with Reactor Core 3.8.7 and no Spring AI, and AI with Spring AI 2.0.1, Reactor 3.8.7, and Spring 7.0.9. The worker report records `jdeps` as `java.base`, exact JAR SHA-256 values, module tree identities, and the test blob, so the support claims are bound to built artifacts rather than declared coordinates alone.
- Requirement: run applicable public HTTP, Local/Remote, and both topology Scenarios at the integrated baseline without creating a new infrastructure Cartesian product. `/private/tmp/execution-context-frontier-20260905/evidence/11/scenario-e055adb/scenario-evidence.json` now records overall `PASS` at exact source head `e055adb...` and tree `3eafab2...`: `microservices` exited 0 in 316.92 seconds and `business-core-monolith` exited 0 in 124.164 seconds. Both logs record the public auth/error/route/tenant/async contract and Kafka stop/recovery PASS; the first run also records the full clean install and focused duplicate-delivery IT PASS, while the second reused the same hashed app artifacts. Source head/tree remained unchanged after each run.
- Requirement: provide one lifecycle guide covering the three standard forms, request-fixed versus shareable objects, actual-thread cleanup, absence/cancel/retry semantics, and unsupported items. `EXECUTION-CONTEXT-PROPAGATION.md:9-87` separates direct-submit capture, registration-time binding, and native carriers, then states fixed ownership, stateless sharing, parent exit, cancellation/timeout, retry, strict absence, and tenant-resource rules. Lines 89-146 name the actual consumers and constrain global Hooks, raw/common/unselected executors, arbitrary SDK/Advisor callbacks, MVC async lifecycles, transactions/connections, Platform messages, and the other excluded models.
- Requirement: synchronize CONTEXT, ADR 0003/0035, and the one propagation ADR, and promote Product Surface only from real consumer evidence. CONTEXT and ADR 0003/0035 already contain the Platform/Tenant and authorization boundaries from predecessor tickets. The range changes only ADR 0037's allocated ticket-11 section (`ADR 0037:327-355`) and Product Surface promotes only the public seams tied to Notes, Spring task, Reactor, and AI consumer runs (`SCAFFOLD-PRODUCT-SURFACE.md:3-19,25-35`). WebFlux request entry and complete MVC async-return integration remain Planned (`SCAFFOLD-PRODUCT-SURFACE.md:42-46`); Kafka producer authentication/ACLs and external AI providers remain explicitly outside the evidence.
- Requirement: preserve the parent spec and original finding lifecycle and do not treat ticket evidence as formal final PASS. Neither lifecycle artifact changed. The worker report explicitly leaves independent review, Normal quality gate, final acceptance, tracker updates, and parent/finding disposition to their owning stages. This Spec review establishes only a clean Spec axis for the stated fixed base/head; it grants no tracker, release, integration, publish, or deployment result.

## Current evidence and limits

The exact-head affected verification log records `BUILD SUCCESS`, 237 tests, zero failures/errors/skips, and current runs of Notes (including the two compositions), Spring task, Reactor, AI, and their applicable dependency modules. The focused Notes run separately records 2/2 PASS against real PostgreSQL; the initial sandbox attempt failed at Docker discovery before assertions, and the authorized identical rerun succeeded. Saved dependency trees, JAR/tree/test identities, Markdown-link validation, and diff hygiene are recorded in the worker report.

C15 is terminal `PASS`, not pending: both topology entries in `scenario-evidence.json` have exit code 0 and exact matching source-before/source-after head and tree. This review did not run the repository quality gate or final acceptance and does not claim either stage.
