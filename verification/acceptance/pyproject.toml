[project]
name = "moduvera-reference-acceptance"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["pytest==8.4.2"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = ["--strict-markers", "--strict-config"]
