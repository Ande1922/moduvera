#!/usr/bin/env bash
set -euo pipefail

HOOK_DIR="$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)"
REPO_ROOT="$(CDPATH= cd -- "$HOOK_DIR/.." && pwd)"
exec python3 "$REPO_ROOT/tools/git-hooks/hooks.py" pre-push "$@"
